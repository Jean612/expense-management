import { Montserrat, Lacquer, Dosis, Doto } from "next/font/google";

export const montserrat = Montserrat({ subsets: ['latin'] });
export const lacquer = Lacquer({ subsets: ['latin'], weight: "400" });
export const dosis = Dosis({ subsets: ['latin'] });
export const doto = Doto({ subsets: ['latin'] });